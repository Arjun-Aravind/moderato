# Development docker-compose configuration
# Usage: docker-compose -f docker-compose.dev.yml up

version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: moderato-redis-dev
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --loglevel debug
    volumes:
      - ./data/redis:/data
    networks:
      - moderato-dev

  redis-insight:
    image: redislabs/redisinsight:latest
    container_name: moderato-redis-insight
    ports:
      - "8001:8001"
    volumes:
      - ./data/redisinsight:/db
    networks:
      - moderato-dev
    depends_on:
      - redis

networks:
  moderato-dev:
    driver: bridge
